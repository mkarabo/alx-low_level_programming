#include "main.h"
#include<stdio.h>
void _puts(char *str);
/**
 * main - Entry point
 * _puts - prints
void _puts(char *str) * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
